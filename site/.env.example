# Environment variables for the TLE Sync documentation site

# Default theme (optional, defaults to 'nightfall')
DEFAULT_THEME=nightfall

# Build-time environment variables (set by CI)
PUBLIC_COMMIT_SHA=
PUBLIC_DEPLOY_TIME=

# GitHub Token for data fetching (CI only, not exposed to client)
GITHUB_TOKEN=
